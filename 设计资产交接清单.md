# 设计资产交接清单 · 国漫笔记重启

> **文档类型**：设计 → 前端交付物（Design Handoff）
> **版本**：v1.0 · 2026-08-03
> **来源载体**：Ardot 设计画布 `710902448582301`（设计系统 v6.0 + 复用组件库 + 7 屏，已全部组件化、`capture_layout` 零问题）
> **目标读者**：Phase 3 前端开发（Next.js / React）
> **目的**：把设计意图沉淀为**可直接消费的前端规范**，前端落地时 1:1 对齐，锁死里程碑 **M3（设计落地偏离设计稿）** 风险。
> ⚠️ 所有 Node ID 以画布最新状态为准（组件化过程中偶有重排，请以「组件名」检索画布）。

---

## 1. 设计 Token 总表（单一真相源）

前端**必须**把这些值落进 `tailwind.config.ts` 的 `theme.extend` 或 CSS 变量，**禁止**在业务代码里写死 hex。

### 1.1 颜色 Token

| Token | 值 | 用途 | Tailwind key 建议 |
|---|---|---|---|
| bg-root | `#0A0A0C` | 页面根背景 | `bg-root` |
| bg-card | `#141417` | 卡片背景 | `bg-card` |
| bg-raised | `#1C1C20` | 浮起元素（次按钮、下拉等） | `bg-raised` |
| border-subtle | `#27272A` | 卡片描边 / 分隔线 | `border-subtle` |
| text-primary | `#EFEBE2` | 主文字 | `text-primary` |
| text-secondary | `#A8A298` | 次要文字 / 标签 | `text-secondary` |
| text-tertiary | `#6B6760` | 弱提示 | `text-tertiary` |
| accent | `#FF5C3A` | 主行动 / active 文字 | `accent` |
| accent-bg | `#1E130D` | active 底（导航选中态） | `accent-bg` |
| success | `#4ADE80` | 已发布 / 正向增量 | `success` |
| warning | `#FBBF24` | 待发布 / 警示增量 | `warning` |
| 头条 | `#FF4D4F` | 平台标识（今日头条） | `plat-toutiao` |
| 百家 | `#3B82F6` | 平台标识（百家号） | `plat-baijia` |
| B站 | `#FB7299` | 平台标识（B 站） | `plat-bilibili` |
| 小红书 | `#FF2442` | 平台标识（小红书） | `plat-xhs` |

### 1.2 Tailwind 映射（直接复制）

```ts
// tailwind.config.ts
import type { Config } from "tailwindcss";

export default {
  content: ["./app/**/*.{ts,tsx}", "./components/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        root: "#0A0A0C",
        card: "#141417",
        raised: "#1C1C20",
        subtle: "#27272A",
        primary: "#EFEBE2",
        secondary: "#A8A298",
        tertiary: "#6B6760",
        accent: "#FF5C3A",
        "accent-bg": "#1E130D",
        success: "#4ADE80",
        warning: "#FBBF24",
        plat: {
          toutiao: "#FF4D4F",
          baijia: "#3B82F6",
          bilibili: "#FB7299",
          xhs: "#FF2442",
        },
      },
      fontFamily: {
        sans: ['"Noto Sans SC"', "system-ui", "sans-serif"],
      },
      borderRadius: {
        card: "14px",
        nav: "8px",
        row: "8px",
        btn: "8px",
        pill: "20px",
      },
      spacing: {
        // 4-up 网格间距
        gap4: "16px",
      },
      maxWidth: {
        // 主内容列
        content: "976px",
      },
    },
  },
} satisfies Config;
```

### 1.3 字体规范

| 角色 | 字重 | 字号参考 | 说明 |
|---|---|---|---|
| 标题 | SemiBold (600) | 标题/数值 30、卡片 value 30、section 18 | `font-semibold` |
| 导航 | Medium (500) | 14 | `font-medium` |
| 正文 | Regular (400) | 13–14 | `font-normal` |

- **统一 `Noto Sans SC`**（中文），禁止默认 Inter。
- 引入方式：Google Fonts 或自托管 woff2，`<html lang="zh-CN">` + `font-sans` 全局套用。

### 1.4 圆角 / 栅格

| 项 | 值 |
|---|---|
| 卡片圆角 | 14 |
| 导航项 / 表格行 / 按钮圆角 | 8 |
| 状态药丸圆角 | 20（全圆角） |
| 主内容列宽 | **固定 976px**（居中，左右留白自适应） |
| 4-up 卡宽 | `(976 − 3×16) / 4 = 232px` |
| 卡片内边距 | 参考 comp-card 232×116，内边距约 16–20 |

---

## 2. 复用组件库清单（reusable 主组件）

> 全部 `reusable: true` 主组件。前端应做成**等价 React 组件**，改一处全局生效。
> Node ID 见各组件，仅供在画布定位参考。

| 组件 | 画布 Node ID | 规格 | 出现屏 / 实例数 |
|---|---|---|---|
| **comp-card** | `2:273` | 232×116 / 圆角14 | KPI 卡：工作台 / 数据看板 / 配图管理（每屏 4 个） |
| **comp-button** | `6:69` | 主按钮，accent 填充，圆角8 | 新建文章等主行动 |
| **comp-button-secondary** | `6:71` | 描边中性（border-subtle），圆角8 | 次行动 |
| **comp-table-row** | `2:277` | 976×61 / 圆角8，5 列 | 文章管理表格行 |
| **comp-nav-item** | `6:105` | 200×40 / 圆角8，图标嵌套 ref slot | 全 7 屏侧边栏（42 实例） |
| **ic-dashboard** | `6:74` | 20×20 线性描边 `#A8A298` | 导航图标：工作台 |
| **ic-pen** | `6:80` | 同上 | 导航图标：AI 写作 |
| **ic-doc** | `6:84` | 同上 | 导航图标：文章管理 |
| **ic-calendar** | `6:89` | 同上 | 导航图标：周计划 |
| **ic-chart** | `6:93` | 同上 | 导航图标：数据看板 |
| **ic-image** | `6:97` | 同上 | 导航图标：配图管理 |
| **ic-folder** | `6:102` | 同上 | 导航图标：项目文件 |

### 2.1 组件详细规格

**comp-card（KPI 卡）**
- 结构：label（13 / Regular / text-secondary）· value（30 / SemiBold / text-primary）· delta（12 / success 或 warning 或 text-secondary）
- 语义色：正向增量用 `success`(#4ADE80)，警示/待发布用 `warning`(#FBBF24)，中性用 `text-secondary`
- 背景 `bg-card`，描边 `border-subtle`，圆角 14

**comp-button / comp-button-secondary**
- 主按钮：背景 `accent`(#FF5C3A)，文字 `#0A0A0C` 或 `#EFEBE2`（与 accent 对比足够），圆角 8，padding 约 8×16
- 次按钮：透明底 + `border-subtle` 描边，文字 `text-primary`，圆角 8
- 字号 14 / Medium

**comp-table-row（文章管理表格行）**
- 5 列（顺序）：状态药丸 · 标题 · 平台 · 阅读量 · 日期
- 行高 61，圆角 8，背景 `bg-card`；hover 可微提亮（不强制）
- 状态药丸（pill，圆角20）：
  - 已发布 → `success` 底或文字
  - 待发布 → `warning`
  - 草稿 → `text-tertiary` / `border-subtle`
  - 失败 → `plat-toutiao`(红) 或 `#FF4D4F`

**comp-nav-item（侧边栏导航）**
- 200×40，圆角 8；左图标 slot（20×20）+ 右 label（14 / Medium）
- **active 态**：背景 `accent-bg`(#1E130D) + 文字 `accent`(#FF5C3A) + 图标描边转 accent；非 active 文字 `text-secondary`、图标 `#A8A298`
- 图标通过切换主组件（mainComponent）实现 7 种，前端对应 7 个图标组件或 SVG 集合

---

## 3. 7 屏 × 组件使用映射

统一外壳：**左侧边栏（7 个 comp-nav-item）+ 顶栏（标题 + 搜索 + 新建按钮）**；主内容列固定 976px。

| 屏 | 画布 Node ID | 使用的复用组件 | 屏内定制结构（非组件，前端自行实现） |
|---|---|---|---|
| 工作台 Dashboard | `2:290` | 侧栏 nav×7、comp-card×4（KPI）、comp-button×1 | 近期文章表（= comp-table-row 思路，可复用）、待发布队列卡 |
| 文章管理 Articles | `3:23` | 侧栏 nav×7、comp-table-row×4（含增强「阅读量」列） | 筛选 chips、表头 |
| 周计划 Weekly | `3:185` | 侧栏 nav×7 | 周看板 7 列（周五列 `#1A1410`+accent 描边高亮）、每日任务卡 |
| 数据看板 Analytics | `3:330` | 侧栏 nav×7、comp-card×4（KPI） | 趋势 SVG polyline、平台分布 SVG bars |
| AI 写作 Writer | `4:1` | 侧栏 nav×7、comp-button×1 | 左设置（选题/大纲/平台 chips）、右实时预览（1920 字） |
| 配图管理 Assets | `4:147` | 侧栏 nav×7、comp-card×4（KPI） | 筛选 chips、素材网格 12 卡（thumbnail + 说明） |
| 项目文件 Files | `4:298` | 侧栏 nav×7 | 工具栏（排序 chips）、文件列表 10 行 |

> **注意**：文章管理表格行的「阅读量」列是**组件化时增强**进去的（原 comp-table-row 仅 4 列），前端复刻 comp-table-row 时必须保留 5 列（状态/标题/平台/阅读量/日期），不可丢。

---

## 4. 配图 / 素材资产约定

- **配图命名规则**：`作品名_用途.jpeg`（无日期，简洁）。例：`凡人修仙传_慕兰之战`、`沧元图_孟川立绘`。
- **文章内占位符格式**：`【配图N：作品名_用途】`（用 `作品名_用途` 描述，不用冗长句式）。前端渲染时按 `image_sources` 映射替换为真实图。
- **image_sources key 规则**：`{"【配图1：作品名_用途】": "第N集/时间戳"}`。
- **真实配图缺失时**：用概念图 / 占位块兜底，占位块**不得内联 `onclick` JS**（审计坑 7 字符串注入），文案「请从素材库选择」，并对动态内容 `html.escape`。
- **素材库规模基线**：825 张 / 跨 47 篇；分类占比 已分类 612(74%) / 待分类 213。
- **响应式图**：前端用 Pillow 生成的缩略图（禁用 macOS `sips`），统一圆角 8。

---

## 5. Phase 3 验收清单（前端交付前逐项打勾）

**Token 对齐**
- [ ] `tailwind.config.ts` 颜色与 §1.1 逐值一致（含 4 平台色）
- [ ] 全局 `Noto Sans SC`，无 Inter 残留
- [ ] 主内容列 976px、4-up 卡 232px、栅格间距 16px

**组件对齐**
- [ ] 7 个复用组件做成等价 React 组件，传入 props 即可换内容
- [ ] comp-card / comp-button / comp-table-row / comp-nav-item 规格与 §2.1 一致
- [ ] 导航 active 态（accent 底 + accent 文字）正确切换
- [ ] 文章管理表格行保留 5 列（含阅读量）

**7 屏对齐**
- [ ] 统一外壳（侧栏 7 nav + 顶栏）在 7 屏一致
- [ ] 各屏真实数据基线（见 §3 与《国漫笔记重启方案.md》§2.4）正确呈现
- [ ] 周五列高亮（`#1A1410` + accent 描边）在周计划生效
- [ ] 小红书主战场标识、素材库 825 等数字一致

**质量**
- [ ] 无横向溢出 / 裁剪（等价 `capture_layout` 零问题，可写脚本断言）
- [ ] 输入全部转义（无 XSS / 注入）
- [ ] 视觉复核：用户在多模态下签字通过（间距 / 字体渲染 / placeholder 图）

---

## 6. 关联文档索引

- 设计稿（Ardot）：`710902448582301` —— 设计系统 v6.0 + 复用组件库 + 7 屏
- 合并方案：《国漫笔记重启方案.md》§2 设计稿（Token / 信息架构 / 组件库 / 真实数据基线）
- 任务拆解：《开发任务拆解.md》Epic 3.1–3.3（前端工程 / 七屏实现 / 视觉复核）
- 后端规则源：`rebuild/backend/config/platforms.yaml`（标题字数 / 小红书纯文字无图 —— 前端质检也只读这份）

*交接清单输出日期：2026-08-03 · 设计助手产出，供 Phase 3 前端直接消费*
